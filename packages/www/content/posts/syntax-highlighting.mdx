---
title: Testing Syntax Highlighting
date: 2019-01-01
---

In this post I'm gonna test out the syntax highlighting I can achieve with [Prism.js](https://prismjs.com/).

## TypeScript

```typescript
/** 
 * This is a comment
*/

function hello(woah: any) {
    lala((blah: ReallyCoolInterface) => {
        let x = foo ? bar : baz;
        const bool = false
    })
}

class Greeter extends Usher {
    hello(greeting: string) {
        return `Hello, ${greeting}` // This is also a comment
    }

    goodbye(name) {
        return "Goodbye " + name + "."
    }
}

```

## JSX
```jsx
/** @jsx jsx */

import { jsx } from 'theme-ui'

export default ({ posts }) => (
    <section>
        {posts.map(post =>
            <article>post</article>
        )}
    <section/>
)
```

## bash

```shell
➜ ls
lerna.json  node_modules  package-lock.json  package.json  packages  yarn.lock
```

## Rust

 ```rust
 enum Coin {
    Penny,
    Nickel,
    Dime,
    Quarter,
}

fn value_in_cents(coin: Coin) -> u8 {
    match coin {
        Coin::Penny => 1,
        Coin::Nickel => 5,
        Coin::Dime => 10,
        Coin::Quarter => 25,
    }
}
```
